import { ProductListRequest } from './product-list-request';

describe('ProductListRequest', () => {
  it('should create an instance', () => {
    expect(new ProductListRequest()).toBeTruthy();
  });
});
