import { ProductSearchRequest } from './product-search-request';

describe('ProductSearchRequest', () => {
  it('should create an instance', () => {
    expect(new ProductSearchRequest()).toBeTruthy();
  });
});
