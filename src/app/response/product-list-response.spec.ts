import { ProductListResponse } from './product-list-response';

describe('ProductListResponse', () => {
  it('should create an instance', () => {
    expect(new ProductListResponse()).toBeTruthy();
  });
});
